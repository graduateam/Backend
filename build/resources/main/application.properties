spring.config.import=classpath:properties/env.properties


# ?????? ??
spring.application.name=Graduate

# MySQL ?????? ?? ??
spring.datasource.url=jdbc:mysql://localhost:3306/graduate_db?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=${mysql.username}
spring.datasource.password=${mysql.password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA ??
spring.jpa.hibernate.ddl-auto=update  # ?? ??? 'update', ?? ????? 'validate'
spring.jpa.show-sql=true  # ???? SQL ??? ??? ??
spring.jpa.properties.hibernate.format_sql=true  # SQL ?? ? ???

# MySQL ??? ? ?? (?? ??)
spring.datasource.hikari.maximum-pool-size=10  # ?? ??? ? ???
